const express = require("express") //this import system is for backend
const app = express() //instantiate/ express and put it in a variable

//you can type any port that you want, besides the reserved/well-known ones
app.listen(3000);